# 项目进度表（本地网页版）

这是一个无需安装依赖的本地网页。直接双击 `index.html` 即可打开。

功能：

- 搜索、阶段筛选、按日期排序
- 添加项目记录
- 分组折叠与行选择
- 点击阶段标签可移除该状态
- 数据自动保存在浏览器 `localStorage`
- 适配窄屏与移动端

如需清空演示数据，可在浏览器开发者工具中删除站点的 `projectRecords` 本地存储项后刷新。
